# Infrastructure

This directory is reserved for local development and integration infrastructure.

Docker Compose is a local development and integration aid only. It must not force the frontend, backend, and RAG service to be deployed together. Each application remains independently deployable.

Future infrastructure may include PostgreSQL, backend, RAG service, a vector database, monitoring, CI configuration, and supporting scripts.
