# API

API design will be defined in later tasks.

The frontend must call the backend API only. The backend will decide whether a request should be handled with a structured database query or delegated to the RAG service after authorization checks.

No API endpoints are implemented in T001.
